# The mock interaction spike works without Supabase.
NEXT_PUBLIC_POCKET_MODE=mock

# Add these when implementing anonymous auth + multiplayer rooms.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Domain-specific token, if your WebMCP testing setup requires the origin trial.
WEBMCP_ORIGIN_TRIAL_TOKEN=
